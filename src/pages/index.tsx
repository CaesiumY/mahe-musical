import HeroSection from "@/components/main/HeroSection";
import { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
      <HeroSection />
    </div>
  );
};

export default Home;
